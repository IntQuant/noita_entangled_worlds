#! /usr/bin/env nix
#! nix develop ../..#cross-noita_proxy --command bash

# You can pass any arguments to this script which will be passed to cargo
# e.g. `./build-noita_proxy --release`

cargo build --target=x86_64-pc-windows-gnu "$@"
